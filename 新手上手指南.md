# Autoresearch 新手上手指南（中文）

## 1. 先理解这个项目在做什么

这个仓库的核心不是“写一个模型就结束”，而是做一个**自动科研循环**：

1. 固定评测标准。
2. 每次只改一点点代码。
3. 自动跑一次训练。
4. 指标变好就保留，变差就回退。
5. 重复很多轮，持续自优化。

一句话：它是一个“会自己迭代实验”的科研流水线。

## 2. 仓库里每个文件的职责

- `src/prepare.c`：准备阶段。初始化缓存、读取语料、构建词表。
- `src/train.c`：训练/评估入口。按时间预算运行并输出指标。
- `src/ar_common.c`：共享逻辑（分词、词表、路径、读写）。
- `program.md`：通用实验协议。
- `program_vehicle_noise.md`：车辆噪声抑制任务迁移模板。
- `results.tsv`：每次实验结果记录表（你自己维护）。

## 3. 第一次跑通（5分钟版）

```bash
make clean && make -j
make run-prepare
./bin/train --time-budget 60
```

看到 `val_bpb`、`training_seconds` 等输出，说明流水线可用。

## 4. 正确的实验节奏

每轮实验只做一件事，例如只改一个超参数：

1. 修改 `src/train.c`（只改一个点）。
2. 重新编译：`make -j`。
3. 运行：`./bin/train --time-budget 300 > run.log 2>&1`。
4. 提取指标：`grep "^val_bpb:" run.log`。
5. 记录到 `results.tsv`。
6. 如果变好：保留并提交；如果变差：回退。

## 5. 新手最容易踩的坑

1. 一次改太多，导致不知道是哪处带来提升。
2. 评测标准改来改去，实验不可比。
3. 只看单次最好结果，不看整体趋势。
4. 不写实验记录，几天后无法复盘。

## 6. 推荐你的前两周学习路线（EECS大二友好）

1. 第1-2天：只跑通流程，理解输入输出。
2. 第3-5天：做 10 轮“小改动实验”，练记录与回退。
3. 第2周：把一个你熟悉的任务迁移进来（如噪声抑制），跑出可重复的基线 + 改进结果。

## 7. 你现在可以直接做的事

1. 先用 `run_once.sh` 把单轮流程自动化。
2. 建一个 `results.tsv`，开始记录你的前 20 轮实验。
3. 按“单变量改动”原则迭代，不追求一步到位。
