# 科研项目迁移指南（中文）

本指南讲如何把“任意科研问题”迁移到 autoresearch 架构中，形成自动自优化循环。

## 1. 迁移核心原则

把项目拆成四层：

1. 不可改层（Harness）
- 数据切分
- 评测代码
- 时间预算
- 硬件约束

2. 可改层（Search Space）
- 模型结构
- 超参数
- 损失函数
- 特征工程

3. 决策层（Policy）
- improve -> keep
- worse -> discard
- crash -> log

4. 记录层（Ledger）
- 每轮一条实验记录（commit + 指标 + 结论）

## 2. 标准迁移流程

1. 定义单命令运行接口（例如 `./bin/train --time-budget 300`）。
2. 固定输出格式（关键指标必须稳定可解析）。
3. 建立 `results.tsv` 表头。
4. 规定每轮只改一个变量。
5. 配置自动化脚本（如 `run_once.sh`）。
6. 连续跑 20~50 轮，形成优化轨迹。

## 3. 车辆噪声抑制示例（RNN替代滤波器）

### 3.1 目标定义

主目标：提升语音质量与可懂度。

建议指标：
- `snri`（越高越好）
- `pesq`（越高越好）
- `stoi`（越高越好）

硬约束：
- `latency_ms <= 10`
- `cpu_percent <= 60`

### 3.2 代码边界建议

- `prepare`：数据切分、特征提取（只读）。
- `eval`：指标计算（只读）。
- `train`：模型与训练策略（可改）。

### 3.3 每轮实验模板

1. 只改一项（如 hidden size: 64 -> 96）。
2. 运行固定预算训练。
3. 解析指标并写入 `results.tsv`。
4. 先看约束是否过线，再看主指标。
5. 过线且变好才保留。

### 3.4 可搜索空间（建议）

1. RNN类型：GRU/LSTM
2. 隐层维度：32/64/96/128
3. 层数：1/2/3
4. 帧长与hop
5. 损失组合（时域 + 频域）
6. 因果/非因果结构
7. 量化策略（部署导向）

## 4. 高质量迁移的判断标准

1. 同样预算下，指标持续提升。
2. 结果可重复，不靠偶然随机种子。
3. 记录完整，能回放每次决策。
4. 能明确解释“为什么这次改动有效”。

## 5. 一份可直接使用的结果表头

```text
commit\tsnri\tpesq\tstoi\tlatency_ms\tcpu_percent\tstatus\tdescription
```

## 6. 给学生做科研最实用的建议

1. 先追求“流程稳定”，再追求“指标惊艳”。
2. 先让实验自动跑起来，再谈复杂模型。
3. 保持小步快跑和强记录习惯，这是科研复利的来源。
